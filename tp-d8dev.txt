TP - Drupal 8 Dev



Etape 2 : 
- Ajouter un hook uninstall dans notre module pour supprimer la config : le code pour supprimer de la config est celui la : 
  // Using \Drupal::configFactory()
  \Drupal::configFactory()->getEditable('automated_cron.settings')->delete();
  // Or \Drupal::service.
  \Drupal::service('config.factory')->getEditable('automated_cron.settings')->delete();
- Créer page pour préparer l'affichage d'un webservice de méteo (créer le routing plus le controller) 


Etape 3 : Création d'un bloc et travail avec les BDD
- Créer un bloc custom qui ne s'affichera que sur les contenus de type Bateau
- Celui-ci contiendra le calcul de l'aire du bateau (en prenant l'hypothèse que le bateau est 
rectangulaire, donc le calcul est largeur * longueur)

Etape 3 : Permission et Envoi d’email à partir des données de l'entité
- Créer une permission pour accéder au bloc de calcul de l'air (permission statique)
- Envoyer un email au propriétaire d’un bateau en cas de modification de sa fiche 
(utilisation du hook form_alter et de l'API des entités)
- Ne permettre que au propriétaire du bateau de modifier la fiche du bateau dont il est propriétaire 

Etape 4 : création d'un formulaire d'import
- Créer un formulaire d’import de fichier CSV
- Créer la route pour ce formulaire
- Ajouter les contenus bateau à partir du fichier

Etape 5 : Ajouter des informations aux vues
- Créer une vue listant mes bateaux sous la forme d'une page avec l'URL /mes-bateaux
- Cette vue affichera une grille des bateaux avec l'ensemble des informations disponible sur un bateau
- Ajouter un champ aire aux vues qui calcule l’aire d’un bateau
- Déclarer ce champs sous la forme d’un plugin

Etape 6 : Déclaration de template
- Créer un template twig pour la fiche d’un bateau 
le positionner dans le dossier templates (à créer) de notre module
- Déclarer ce template à l’aide du hook_theme 

Bonus :
- Créer un formulaire permettant de faire un calcul d'aire et l'afficher dans un bloc qui apparait sur toutes les cases
- Lors du calcul de l'aire sur une fiche bateau, ajouter la valeur de l'aire dans le contenu
stocké en base de donnée
- Optionnel- Stocker également ce résultat dans la table (non drupal) d'une deuxième base de données
